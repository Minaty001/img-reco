# ========================================
# Vision Assistant - Python Dependencies
# ========================================
# Python 3.13 compatible
# Designed for Render.com & Android Termux

# Web Framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Image Processing
Pillow>=10.0.0
python-multipart>=0.0.9

# YOLOv8 Object Detection
# Ultralytics auto-downloads the model on first use
ultralytics>=8.3.0

# Utilities
numpy>=1.26.4

# PyTorch (CPU-only is sufficient for inference)
# Render.com build includes torch automatically via ultralytics dependency
# No need to pin a specific torch version
